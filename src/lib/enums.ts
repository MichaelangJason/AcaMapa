export enum ResultType {
  DEFAULT = "default",
  COURSE = "course",
  PROGRAM = "program",
  AI = "ai",
}

export enum Language {
  EN = "en",
  FR = "fr",
}

export enum DraggingType {
  TERM = "term",
  COURSE = "course",
  PLAN = "plan",
}
